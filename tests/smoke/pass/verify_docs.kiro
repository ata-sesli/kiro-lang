import io

/// Adds two numbers.
/// Returns the sum.
pure fn add(a: num, b: num) -> num {
    return a + b
}

/// A User struct.
struct User {
    name: str
    age: num
}

fn main() {
    io.print(add(10, 20))
    io.print("Docs parsed successfully!")
}
main()
