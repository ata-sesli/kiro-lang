import math
import io


fn main() {
    io.print("Testing Imports...")
    var sum = math.add(10, 20)
    io.print(sum)

    var diff = math.sub(sum, 5)
    io.print(diff)
    loop num in 0..100 per 2.5 {
    }
}

main()
