import io

pure fn add(a: num, b: num) -> num {
    return a + b
}

fn main() {
    io.print("Start")

    // 1. Sync Call from Async context
    var res = add(10, 20)
    io.print(res)

    // 2. Async wrapper (Run) of pure function
    run add(30, 40)

    io.print("Done")
}
main()
