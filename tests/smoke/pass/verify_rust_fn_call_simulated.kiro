import io

// Test that a rust fn call has matching adjacent host glue.

error NotFound = "File not found"

rust fn read_file(path: str) -> str!

fn main() {
    io.print("Testing interpreter simulation of rust fn calls...")

    var content = read_file("test.txt")
    io.print(content)

    io.print("Simulator call completed")
}

main()
