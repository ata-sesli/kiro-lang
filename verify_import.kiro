// Verify standard library import (std_fs) and mocking
import std_fs

print "Imported std_fs."

// Try calling a mock function (host function)
var content = std_fs.read_file("test.txt")
print "Read Content: " + content

// Try importing a local module (create dummy first)
import mylib
var p = mylib.pi()
print "Local Lib PI: " + p
